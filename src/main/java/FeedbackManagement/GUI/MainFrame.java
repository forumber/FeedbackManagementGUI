/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package FeedbackManagement.GUI;

import FeedbackManagement.MainApplication;

/**
 *
 * @author Berkay
 */
public class MainFrame extends javax.swing.JFrame {
    
    public MainApplication application;

    /**
     * Creates new form MainFrame
     */
    public MainFrame(MainApplication application) {
        this.application = application;
        initComponents();        
    }
    
    public void start()
    {
        changePanel("LoginRegisterSelection");
    }

        /**
     * Changes the current panel by removing all objects from frame and recreate them.
     * @param panelName panel name to get replaced into frame.
     */
    public void changePanel(String panelName)
    {
        if (panelName.equalsIgnoreCase("LoginRegisterSelection"))
        {
            getContentPane().removeAll();
            getContentPane().add(new LoginRegisterSelectionPanel(this));
            pack();
            repaint();
            setSize(300, 300);
        } 
        
        else if (panelName.equalsIgnoreCase("Register"))
        {
            getContentPane().removeAll();
            getContentPane().add(new RegisterPanel(this));
            pack();
            repaint();
            setSize(270, 400);
        } 
        
        else if (panelName.equalsIgnoreCase("Login"))
        {
            getContentPane().removeAll();
            getContentPane().add(new LoginPanel(this));
            pack();
            repaint();
            setSize(300, 150);
        } 
        setLocationRelativeTo(null);
        setVisible(true);
    }
    
    /**
     * This method is called from within the constructor to initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is always
     * regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        setDefaultCloseOperation(javax.swing.WindowConstants.EXIT_ON_CLOSE);
        setTitle("FeedBackManagementApp");
        setResizable(false);

        pack();
    }// </editor-fold>//GEN-END:initComponents

    // Variables declaration - do not modify//GEN-BEGIN:variables
    // End of variables declaration//GEN-END:variables
}
